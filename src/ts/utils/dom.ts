/**
 * fixme 这个方法还有bug用不了
 * 获取自定义元素的自定义属性
 * @param el: Element
 */
export const getElementCustomProps = (el: Element): string[] => {
    // const newEl = document.createElement('interactive-player');
    // return Object.keys(Object.getPrototypeOf(el)).filter(
    //     (prop) => !(prop in newEl)
    // );
    return [];
};
